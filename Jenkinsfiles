pipeline {
    agent any

    environment {
        IMAGE_NAME = 'myapp:latest' // Docker image name
    }

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'main', url: 'https://github.com/Ishwarya-git-source/Ownimage.git' // Replace with your repository URL
            }
        }

        stage('Build Application') {
            steps {
               bat 'docker build -t ${IMAGE_NAME} .' // Build Docker image
            }
        }

        stage('Run Tests') {
            steps {
                bat 'docker run --rm ${IMAGE_NAME} go test ./...' // Run tests inside the container
            }
        }

        stage('Push to Registry') {
            steps {
                withDockerRegistry([credentialsId: 'docker-credentials', url: 'https://hub.docker.com/r/ishwarya2001/secondimage/tags']) {
                  bat 'docker push ${IMAGE_NAME}' // Push image to registry
                }
            }
        }

        stage('Deploy') {
            steps {
                bat 'docker run -d -p 8080:8080 ${IMAGE_NAME}' // Deploy the application
            }
        }
    }
}
